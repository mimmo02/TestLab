# TestLab — dipendenze Python
# Installa con: pip install -r requirements.txt

# Dati
pandas>=2.0
numpy>=1.26

# Visualizzazione
plotly>=5.20
ipywidgets>=8.1
jupyterlab>=4.0

# Encoding detection (per CSV con charset sconosciuto)
chardet>=5.0

# Export report
jinja2>=3.1
openpyxl>=3.1

# DVC (data version control) — scegli il backend che usi:
dvc>=3.0
# dvc[s3]        # per AWS S3
# dvc[gdrive]    # per Google Drive
# dvc[ssh]       # per server SSH / GitLab via SSH
